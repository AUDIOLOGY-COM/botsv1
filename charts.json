{
  "openapi": "3.0.2",
  "info": {
    "version": "1.0.0",
    "title": "Charts API",
    "description": "API for creating, retrieving, updating, and deleting charts.\n\nSome chart API property names differ from the option labels in the UI. These differences are noted for each property name.\n\n## Requirements\n\n  * You must have an organization access token with the API permission or a session token to use the API.\n  * You must have the Splunk Observability Cloud admin or power role to use the `POST /chart`, `PUT /chart/{id}`, and `DELETE /chart/{id}` endpoints.\n  * You must have the Splunk Observability Cloud admin, power, or read_only role to use the `GET /chart` and `GET /chart/{id}` endpoints. \n",
    "x-description": "API for creating, retrieving, updating, and deleting charts.\n\nSome chart API property names differ from the option labels in the UI. These differences are noted for each property name.\n\n## Requirements\n\n  * You must have an organization access token with the API permission or a session token to use the API.\n  * You must have the Splunk Observability Cloud admin or power role to use the `POST /chart`, `PUT /chart/{id}`, and `DELETE /chart/{id}` endpoints.\n  * You must have the Splunk Observability Cloud admin, power, or read_only role to use the `GET /chart` and `GET /chart/{id}` endpoints. \n",
    "x-update-date": "* 2019-06-05\n* 2019-06-07\n* 2019-11-21\n* 2019-11-24\n* 2020-01-16\n",
    "x-version-info": "\n* 2019-06-05: 3.1.0: DOCS-483: Updates for corrected authentication token requirements\n* 2019-06-07: 3.1.0: DOCS-483: Updates for corrected authentication token requirements\n  Simplified description of X-SF-Token\n* 2019-11-21: 3.1.1: cleanup, no separate branch\n* 2019-11-24: 3.2.0: Timestamps in milliseconds are mislabeled as integers/int32\n* 2019-01-16: 3.3.0: DOCS-1033: API Docs - Update docs: Some color palette indices are only available for histogram charts\n"
  },
  "servers": [
    {
      "url": "https://api.{REALM}.observability.splunkcloud.com/v2",
      "description": "Endpoint URL",
      "x-description": "Splunk Observability Cloud API endpoint.\n\nReplace `{REALM}` with the name of your realm.\nFor example, if your realm is `eu0`, use\n`https://api.eu0.observability.splunkcloud.com/v2`.\n\nTo find your realm, go to your\nprofile page in the user interface.\\n If you don't include a realm and\nuse `https://api.observability.splunkcloud.com/v2`, the API interprets the endpoint as\npointing to the `us0` realm.\n"
    }
  ],
  "paths": {
    "/chart": {
      "post": {
        "tags": [
          "Create Single Chart"
        ],
        "summary": "Creates a new chart",
        "description": "Creates a new chart\n",
        "x-description": "Creates a new chart. After you create the chart, add it to a\ndashboard using either the Create Dashboard or Update Dashboard API.\nYou can then view and modify the chart from that dashboard. The chart\nalso appears in the chart catalog for your organization. \n\nFor more information, \nsee [Display data using charts](https://dev.splunk.com/observability/docs/chartsdashboards/charts_overview/).\n",
        "operationId": "Create Single Chart",
        "parameters": [
          {
            "name": "Content-Type",
            "in": "header",
            "x-description": "Format of the request body. Always  `application/json`.\n",
            "schema": {
              "type": "string"
            },
            "required": true
          },
          {
            "name": "X-SF-Token",
            "in": "header",
            "x-description": "Authentication token\n",
            "schema": {
              "type": "string"
            },
            "required": true
          }
        ],
        "requestBody": {
          "x-description": "Chart specifications, in the form of a JSON object\n",
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateUpdateChartRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "",
            "x-description": "Success\n",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CreateUpdateChartResponse"
                }
              }
            }
          }
        }
      },
      "get": {
        "tags": [
          "Retrieve Charts Using Query"
        ],
        "summary": "Gets one or more charts based on selection criteria\n",
        "description": "Gets one or more charts based on the selection criteria specified in the\nquery parameters.\n",
        "x-description": "Gets one or more charts based on the selection criteria specified in the\nquery parameters.\n\nNote: Splunk Observability Cloud returns a maximum of 10,000 objects, even if your organization contains more than 10,000. To learn more, see the\n[Considerations for retrieve operations](https://dev.splunk.com/observability/docs/apibasics/retrieve_data_basics#Considerations-for-retrieve-operations) section in the developer guide.\n",
        "operationId": "Get Charts Using Query",
        "parameters": [
          {
            "name": "X-SF-TOKEN",
            "in": "header",
            "description": "Authentication token\n",
            "x-description": "Authentication token\n",
            "schema": {
              "type": "string"
            },
            "required": true
          },
          {
            "name": "limit",
            "in": "query",
            "description": "Maximum number of charts to return. The default is 50, and Splunk Observability Cloud\nuses this value if you specify an invalid value.\n",
            "x-description": "Maximum number of charts to return. The default is 50, and Splunk Observability Cloud\nuses this value if you specify an invalid value.\n",
            "schema": {
              "type": "integer",
              "minimum": 1,
              "default": 50
            }
          },
          {
            "name": "name",
            "in": "query",
            "description": "A search pattern for the value of the `name` property of a chart.\nYou can use any UTF-8 character in the string, and the API matches\nthe pattern to any part of the name property. For example,\n`name=per` matches the following chart names:\n\n* dropped *per* day\n* 95th *per*centile\n* *per*sonal disk usage\n\nThe following conditions cause the API to match any value of\nthe `name` property:\n\n* String of length 0\n* Omitting the `name` parameter from the query\n",
            "x-description": "A search pattern for the value of the `name` property of a chart.\nYou can use any UTF-8 character in the string, and the API matches\nthe pattern to any part of the name property. For example,\n`name=per` matches the following chart names:\n\n* dropped *per* day\n* 95th *per*centile\n* *per*sonal disk usage\n\nThe following conditions cause the API to match any value of\nthe `name` property:\n* String of length 0\n* Omitting the `name` parameter from the query\n",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "offset",
            "in": "query",
            "description": "Position in results at which the API should start returning data\n",
            "x-description": "Position in the results at which the API should start returning\ncharts.\n\nThe API puts all the results into a 0-indexed array,\nsorted in a manner appropriate for the specified query parameters.\nThe offset tells the API the array index at which it should start\nreturning results.\n\nIf you specify an offset that's greater than\nthe length of the results array, the API doesn't return any values.\n",
            "schema": {
              "type": "integer",
              "minimum": 0,
              "default": 0
            }
          },
          {
            "name": "tags",
            "in": "query",
            "description": "A search pattern for values in the `tags` array property of a chart.\nYou can use any UTF-8 character in the string, and the API matches\nthe pattern to any part of the tags property.\n  \nTo specify more than one tag to search for, add additional `tag` query parameters\nto the URI. The API combines multiple `tag` queries with an\nimplicit `OR`.\n  \nFor example, suppose you want to retrieve the first 25 charts that have following properties:\n\n* name matches \"myChart\"\n* tags=cpu, tags=prod, tags=customer-facing\n\nThe following `curl` statement retrieves the charts:\n\n```\ncurl -i\n--header \"Content-Type: application/json\"\n--header \"X-SF-TOKEN: <AUTHENTICATION_TOKEN>\" \\\n--request GET \\\nhttps://api.<REALM>.observability.splunkcloud.com/v2/chart?limit=25&name=myChart&offset=0&tags=cpu&tags=prod\n  ```\n",
            "x-description": "A search pattern for values in the `tags` array property of a chart.\nYou can use any UTF-8 character in the string, and the API matches\nthe pattern to any part of the tags property.\n\nTo specify more than one tag to search for, add additional `tag` query parameters\nto the URI. The API combines multiple `tag` queries with an\nimplicit `OR`.\n\nFor example, suppose you want to retrieve the first 25 charts that have following properties:\n\n* name matches \"myChart\"\n* tags=cpu, tags=prod, tags=customer-facing\n\nThe following `curl` statement retrieves the charts:\n\n```\ncurl -i\n  --header \"Content-Type: application/json\"\n  --header \"X-SF-TOKEN: <AUTHENTICATION_TOKEN>\" \\\n  --request GET \\\n  https://api.<REALM>.observability.splunkcloud.com/v2/chart?limit=25&name=myChart&offset=0&tags=cpu&tags=prod\n```\n",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Success\n",
            "x-description": "Success\n",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "properties": {
                    "count": {
                      "title": "Number of charts that match the query",
                      "description": "Number of charts that match the query\n",
                      "x-description": "The number of charts that match the specified query. This\nvalue may be different from the number of charts in the\n`results` array property. The `limit` query parameter\ncontrols the number of charts that the request actually\nreturns, and the `offset` query parameter determines the\nposition at which the request starts returning\nparameters. These two factors can affect the actual\nnumber of returned charts.\n",
                      "type": "integer"
                    },
                    "results": {
                      "title": "Charts returned by the request",
                      "description": "Charts returned\n",
                      "x-description": "An array of charts returned by the request. The charts\nmatch the search criteria specified in the query\nparameters of the request.\n",
                      "type": "array",
                      "items": {
                        "$ref": "#/components/schemas/GetChartsResult"
                      }
                    }
                  }
                }
              }
            }
          }
        }
      }
    },
    "/chart/{id}": {
      "get": {
        "tags": [
          "Retrieve Chart Using ID"
        ],
        "summary": "Retrieves a single chart",
        "description": "Retrieve a chart by its ID\n",
        "x-description": "Retrieves the chart identified by the chart ID in the `id` path parameter\n",
        "operationId": "Get Chart Using Chart ID",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "x-description": "The ID of the chart you want to retrieve",
            "schema": {
              "type": "string"
            },
            "required": true
          },
          {
            "name": "X-SF-Token",
            "in": "header",
            "description": "Authentication token\n",
            "x-description": "Authentication token\n",
            "schema": {
              "type": "string"
            },
            "required": true
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "x-description": "Successful retrieval of the chart specified by ID",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/GetSingleChartResponse"
                }
              }
            }
          }
        }
      },
      "put": {
        "tags": [
          "Update Single Chart"
        ],
        "summary": "Updates an existing chart",
        "description": "Updates an existing chart\n",
        "x-description": "Updates the chart identified by the chart ID in the `id` path parameter. \nIf you don't include a property in the request body, the API removes it from the\nchart or resets it to its default state. \n\nNotes:\n\n  * If you change the chart type in `options.type`, the API \n    removes properties that don't apply to the new chart type. \n  * If you change the chart to a new type, then change it back to its \n    original type, you don't have access to the previous type-specific values, \n    so you have to specify them again.\n",
        "operationId": "Update Single Chart",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "x-description": "The ID of the chart you want to update\n",
            "description": "The ID of the chart you want to update\n",
            "schema": {
              "type": "string"
            },
            "required": true
          },
          {
            "name": "Content-Type",
            "in": "header",
            "schema": {
              "type": "string"
            },
            "required": true
          },
          {
            "name": "X-SF-Token",
            "in": "header",
            "description": "Authentication token\n",
            "x-description": "Authentication token\n",
            "schema": {
              "type": "string"
            },
            "required": true
          }
        ],
        "requestBody": {
          "x-description": "Update Chart request body",
          "description": "Update Chart request body",
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateUpdateChartRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Success\n",
            "x-description": "Successful update of the chart specified by ID\n",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CreateUpdateChartResponse"
                }
              }
            }
          }
        }
      },
      "delete": {
        "tags": [
          "Delete Single Chart"
        ],
        "summary": "Deletes an existing chart",
        "description": "Deletes a chart\n",
        "x-description": "Deletes the chart identified by the chart ID in the `id` path parameter.\n",
        "operationId": "Delete Single Chart",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Chart ID\n",
            "x-description": "The ID of the chart you want to update",
            "schema": {
              "type": "string"
            },
            "required": true
          },
          {
            "name": "X-SF-Token",
            "in": "header",
            "x-description": "Authentication token\n",
            "description": "Authentication token\n",
            "schema": {
              "type": "string"
            },
            "required": true
          }
        ],
        "responses": {
          "200": {
            "description": "Success\n",
            "x-description": "Successful deletion\n"
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "SecondaryVisualization": {
        "title": "Secondary visualization options",
        "type": "object",
        "required": [
          "paletteIndex"
        ],
        "properties": {
          "gt": {
            "title": "Indicates the lower threshold of this range, not including the value\nitself.\n",
            "description": "Non-inclusive color range lower threshold\n",
            "x-description": "The lower threshold of a color range, not including the specified\nvalue itself.\n\nNotes:\n\n  * Use only when `options.type` property is `List` or `SingleValue`;\n    otherwise, the API returns an HTTP response code `400`.\n  * The value must be less than the value of `options.colorScale2.lt` or `options.colorScale2.lte` of the same element.\n  * `options.colorScale2.gt` and `options.colorScale2.gte` are mutually exclusive\n",
            "type": "number",
            "format": "float"
          },
          "gte": {
            "title": "Indicates the lower threshold of this range, inclusive\n",
            "description": "Color range lower threshold, inclusive\n",
            "x-description": "The lower threshold of a color range, including the specified value\nitself.\n\nNotes:\n\n  * Use only when `options.type` property is `List` or `SingleValue`;\n    otherwise, the API returns an HTTP response code `400`.\n  * The value must be less than the value of `options.colorScale2.lt` or `options.colorScale2.lte` of the same element.\n  * `options.colorScale2.gt` and `options.colorScale2.gte` are mutually exclusive.\n",
            "type": "number",
            "format": "float"
          },
          "lt": {
            "title": "Indicates the upper threshold of this range, not including the value\nitself.\n",
            "description": "Color range upper threshold, non-inclusive\n",
            "x-description": "The upper threshold of a color range, not including the specified\nvalue itself.\n\nNotes:\n\n  * Use only when `options.type` property is `List` or `SingleValue`;\n    otherwise, the API returns an HTTP response code `400`.\n  * The value must be less than the value of `options.colorScale2.gt` or `options.colorScale2.gte` of the same element.\n  * `options.colorScale2.lt` and `options.colorScale2.lte` are mutually exclusive.\n",
            "type": "number",
            "format": "float"
          },
          "lte": {
            "title": "Color range upper threshold, inclusive\n",
            "description": "Color range upper threshold, inclusive\n",
            "x-description": "The upper threshold of a color range, including the specified value\nitself.\n\nNotes:\n\n  * Use only when `options.type` property is `List` or `SingleValue`;\n    otherwise, the API returns an HTTP response code `400`.\n  * The value must be less than the value of `options.colorScale2.gt` or `options.colorScale2.gte` of the same\n    element.\n  * `options.colorScale2.lt` and `options.colorScale2.lte` are mutually exclusive\n",
            "type": "number",
            "format": "float"
          },
          "paletteIndex": {
            "title": "Color to use for data points in the specified range",
            "description": "data points color palette index\n",
            "x-description": "Index of a color listed in table 5 of the section [Charts color palettes](https://dev.splunk.com/observability/docs/chartsdashboards/charts_overview#Chart-color-palettes).\nThe value is used to color points in the specified range.\n\nNotes:\n\n  * Use only when `options.type` property is `List` or `SingleValue`; \n    otherwise, the API returns an HTTP response code `400`.\n  * Depending on the settings users select for color blindness, they may see\n    the alternate colors listed in table 6 of the section [Charts color palettes](https://dev.splunk.com/observability/docs/chartsdashboards/charts_overview#Chart-color-palettes).\n    of the Charts API guide.\n",
            "type": "integer",
            "minimum": 0,
            "maximum": 21
          }
        }
      },
      "ChartAxes": {
        "title": "Chart axes options",
        "type": "object",
        "properties": {
          "highWatermark": {
            "description": "Max Y value of interest drawn as horizontal line\n",
            "x-description": "A point on the Y axis at which the chart displays a horizontal line,\nindicating the max Y value of interest. Points with a Y value that\nexceeds the high water mark still appear.\n\nNotes:\n\n  * Use only when `options.type` property is `TimeSeriesChart`. Otherwise, the API returns an HTTP response code `400`.\n  * The value must be less than or equal to `options.axes.max` and greater than\n    `options.axes.lowWaterMark` for the corresponding axis.\n",
            "type": "number",
            "format": "float"
          },
          "highWatermarkLabel": {
            "description": "Label for line drawn for max Y value\n",
            "x-description": "A label that's displayed beside the horizontal line indicating the\nhigh water mark.\n\nNotes:\n\n  * Use only when `options.type` property is `TimeSeriesChart`. Otherwise, the API returns an HTTP response code `400`.\n  * If `options.axes.highWaterMark` isn't specified, this label is ignored.\n",
            "type": "string",
            "maxLength": 1000
          },
          "label": {
            "description": "Y axis label\n",
            "x-description": "Label that's displayed for the Y axis of the chart. It appears to\nthe left of axis values on the left axis and to the right of axis\nvalues on the right axis.\n\nNote: Use only when `options.type` property is `TimeSeriesChart`. Otherwise, the API returns an HTTP response code `400`.\n",
            "type": "string"
          },
          "lowWatermark": {
            "description": "Min Y value of interest drawn as horizontal line\n",
            "x-description": "A point on the Y axis at which the chart displays a horizontal line,\nindicating the minimum Y value of interest. Points with a Y value\nthat is less than the low water mark still appear.\n\nNotes:\n\n  * Use only when `options.type` property is `TimeSeriesChart`. Otherwise, the API returns an HTTP response code `400`.\n  * Value must be greater than or equal to `options.axes.min` and less than\n    `options.axes.highWaterMark` for the corresponding axis.\n",
            "type": "number",
            "format": "float"
          },
          "lowWatermarkLabel": {
            "description": "Label for line drawn for max Y value\n",
            "x-description": "A label that's displayed beside the horizontal line indicating the\nlow water mark. \n\nNotes:\n\n  * Use only when `options.type` property is `TimeSeriesChart. Otherwise, the API returns an HTTP response code `400`.\n  * If `options.axes.lowWaterMark` isn't specified, the API ignores this label.\n",
            "type": "string",
            "maxLength": 1000
          },
          "max": {
            "description": "Largest value to display\n",
            "x-description": "Specifies the largest data value to display on the chart. Overrides\noptions.includeZero if the properties are set to incompatible\nvalues.\n\nNotes:\n\n  * Use only when `options.type` property is `TimeSeriesChart`. Otherwise, the API returns an HTTP response code `400`.\n  * The value must be greater than the value of `options.axes.min` for the same `options.axes` element.\n",
            "type": "number",
            "format": "float"
          },
          "min": {
            "description": "Smallest value to display\n",
            "x-description": "Specifies the smallest data value to display on the chart. Overrides\noptions.includeZero if the properties are set to incompatible\nvalues.\n\nNotes:\n\n  * Use only when `options.type` property is `TimeSeriesChart`. Otherwise, the API returns an HTTP response code `400`.\n  * The value must be less than the value of `options.axes.max` for the same `options.axes` element.\n",
            "type": "number",
            "format": "float"
          }
        }
      },
      "CustomProperties": {
        "title": "Custom properties",
        "description": "Metadata object\n",
        "x-description": "User-defined JSON object containing metadata\n",
        "type": "object",
        "additionalProperties": true
      },
      "Description": {
        "title": "Chart description",
        "description": "Chart description\n",
        "x-description": "Extended text description of the chart. This text appears in the **Chart description** text box\nat the top of the **New Chart** screen in the UI.\n",
        "type": "string"
      },
      "Name": {
        "title": "Chart name (displayed)",
        "description": "Chart name\n",
        "x-description": "Short chart name. In the UI, you enter the chart name in the **Untitled Chart** text box\nat the top of the **New Chart** screen.\n",
        "type": "string"
      },
      "PackageSpecifications": {
        "title": "SignalFlow internal field",
        "x-description": "For internal use only.\n",
        "description": "For internal use only.\n",
        "type": "string",
        "default": ""
      },
      "ProgramText": {
        "title": "Chart SignalFlow program",
        "description": "SignalFlow program for chart data\n",
        "x-description": "SignalFlow program that provides the chart data. If you use\nmore than one line of SignalFlow, separate the lines with semicolons\n(;) or newline characters (\\n). See \n[Charts and SignalFlow](https://dev.splunk.com/observability/docs/chartsdashboards/charts_overview#Charts-and-SignalFlow).\n\nNote: Required only for charts with `options.type` of values `Heatmap`, `List`, `SingleValue`, `TableChart`, and `TimeSeriesChart`.\n",
        "type": "string"
      },
      "Tags": {
        "title": "Chart tags",
        "description": "Tags for chart\n",
        "x-description": "An array that contains tag values. You can use tags to search for or\nfilter charts. One use is to tag charts that are in production with the\nvalue `prod`.\n\nNote: You can't have more than 50 tags per chart.\n",
        "type": "array",
        "items": {
          "type": "string"
        },
        "maxItems": 50
      },
      "PublishLabelOptions": {
        "type": "object",
        "title": "SignalFlow plot options",
        "description": "Plot options\n",
        "x-description": "Properties of a single plot in a chart\n",
        "properties": {
          "displayName": {
            "title": "Alternate display name",
            "description": "Alternate plot name, displayed in Data Table\n",
            "x-description": "Specifies an alternate value for the Plot Name column of the Data\nTable associated with the plot. In the UI, this is the\ntext value listed below **Name**, on the right side of the top\nbar in the **Plot Editor**.\n\nNote: Use only when `options.type` is `TimeSeriesChart`,\n`List`, or `SingleValue`.\n",
            "type": "string"
          },
          "label": {
            "title": "`publish()` block label\n",
            "description": "Label of `publish()` object for this plot\n",
            "x-description": "For the SignalFlow program in the `programText` property of a POST\nor PUT request body, `label` maps its `publishLabelOptions` parent\nobject properties to the plot from the `publish()` block that has the\nsame label in its `label=` argument.\n\nFor example, if the SignalFlow program contains the statement\n\n`A = data('demo.trans.count').publish(label='A')`\n\nand the POST request body for creating a chart contains\n\n```\n\"options\": {\n  ...\n  \"publishLabelOptions\": {\n    ...\n    \"label\": \"A\"\n  }\n}\n```\n\nthen Splunk Observability Cloud applies the properties in the `publishLabelOptions` object\nto the plot displayed by the SignalFlow statement.\n\nNote: Use only when `options.type` property is\n`List`, `TimeSeriesChart`, or `SingleValue`; \notherwise, the API returns an HTTP response code `400`.\n",
            "type": "string"
          },
          "paletteIndex": {
            "title": "Plot color",
            "description": "Plot color palette index\n",
            "x-description": "Color to use for this plot, in the form of an integer index into the color table displayed in\ntable 5 in the section [Chart color palettes](https://dev.splunk.com/observability/docs/chartsdashboards/charts_overview#Chart-color-palettes).\nOnly the first 16 entries (0-15) are allowed for this value.\n\nThis property corresponds to the **Plot Color** palette selector that appears in the\n**Visualization Options** pane. To select this pane, click the **Configure plot**\nicon on the right-hand side of the **Plot Editor** options bar.\n\nNotes:\n\n  * Not available when `options.type` is `Heatmap` or `Text`. For these\n    types, the API returns an HTTP response code `400`.\n  * When `options.type` is `SingleValue` or `List`, or when `options.type` is `TimeSeriesChart` \n    and `options.publishLabelOptions.plotType` is `Histogram`, `paletteIndex` is valid but has no effect on the plot color.\n  * Users may see colors other than those shown in the table, depending on the settings they select for color\n    blindness. To see sample swatches of the alternate colors and the mappings used for\n    color-blind users, see the hexadecimal color codes for the first 16 entries in\n    table 6 in the section [Chart color palettes](https://dev.splunk.com/observability/docs/chartsdashboards/charts_overview#Chart-color-palettes)\n    of the Charts API guide.\n",
            "type": "integer",
            "minimum": 0,
            "maximum": 15
          },
          "plotType": {
            "title": "Plot-specific time series graph visualization",
            "description": "Visualization override for this plot\n",
            "x-description": "Plot-specific override of the time series graph visualization you specify for the entire chart. `plotType` \nonly applies to the SignalFlow statement that provides data for the plot.\n\nFor example, if you create a chart with two SignalFlow statements, set `options.type` to `TimeSeriesChart`, and\nset `defaultPlotType` to `LineChart`, Splunk Observability Cloud displays a chart with two line plots. To change one of the plots\nto a bar chart, set `publishLabelOptions.plotType` for the SignalFlow `publish()` label of the plot to `ColumnChart`.\nThe result is a chart with a line plot and a column plot.\n\nThis property corresponds to the **Visualization Type** selector that appears in the **Visualization Options** pane.\nTo select this pane, click the **Configure plot** icon on the right-hand side of the **Plot Editor** options bar.            \n\nNotes:\n\n  * Not available when the plot type is `Heatmap` or `Text`. For these\n    types, the API returns an HTTP response code `400`.\n  * If you only have one plot for the chart, so that the `publishLabelOptions` array has only one element,\n    this property overrides the plot type for the entire chart.\ntype: string\n",
            "enum": [
              "LineChart",
              "AreaChart",
              "ColumnChart",
              "Histogram"
            ]
          },
          "valuePrefix": {
            "title": "String to prepend to values displayed in a plot",
            "description": "String prepended to values\n",
            "x-description": "Specifies a string to prepend to values displayed in a single value\nor list plot, the data table for a plot, and the tooltip shown\nwhen you hover over a point on a plot. To learn more, see the\nexample in the section \n[Adding a prefix and/or suffix](https://help.splunk.com/?resourceId=data-visualization_charts_chart-builder#df5257940b3044672ae0c2ef3e73a1938__prefix-suffix)\nin the user documentation.\nAny value you set for `options.publishLabelOptions[].valueUnit` property overrides `valuePrefix`.\n\nThis property corresponds to the **Prefix** text box that appears when you select **Custom** from the\n**Display Units** dropdown list in the **Visualization Options** pane. To select this pane, click the\n**Configure plot** icon on the right-hand side of the **Plot Editor** options bar.\n\nNote: Not available when the plot type is `Text`. The API returns an HTTP response code `400`.\n",
            "type": "string"
          },
          "valueSuffix": {
            "title": "String to append to values displayed in charts",
            "description": "String appended to values\n",
            "x-description": "Specifies a string to append to values displayed in a single value\nor list chart, the data table for a chart, and the tooltip shown\nwhen you hover over a point on a chart. To learn more, see the\nexample in the section \n[Adding a prefix and/or suffix](https://help.splunk.com/?resourceId=data-visualization_charts_chart-builder#df5257940b3044672ae0c2ef3e73a1938__prefix-suffix)\nin the user documentation. Any value you set for `options.publishLabelOptions[].valueUnit` property overrides `valueSuffix`.\n\nThis property corresponds to the **Suffix** text box that appears when you select **Custom** from the\n**Display Units** dropdown list in the **Visualization Options** pane. To select this pane, click the \n**Configure plot** icon on the right-hand side of the **Plot Editor** options bar.\n\nNote: Not available when the plot type is `Text`. The API returns an HTTP response code `400`.\n",
            "type": "string"
          },
          "valueUnit": {
            "title": "Specifies the display units to use for chart values",
            "description": "Unit of measure for values\n",
            "x-description": "Specifies the units of measure for values displayed in the plot.\nSplunk Observability Cloud displays values based on the assumption that the raw\ndata has the unit of measure you select. For example, the value 1000 in a\nplot where you've set `options.publishLabelOptions[].valueUnit` to `bits`\nappears as 1 Kilobit, and the value 1024 in a plot set to\n`Bytes` appears as 1 Kebibyte. Splunk Observability Cloud scales units within their\nown unit type. To learn more, see \n[Displaying units and other labels](https://dev.splunk.com/observability/docs/chartsdashboards/charts_overview/#Display-units-and-other-labels-in-graph-charts).\nThe `options.publishLabelOptions[].valueUnit` option applies to scaling\nand labeling in chart tooltips, axes, and data tables. `options.publishLabelOptions[].valueUnit` overrides `options.publishLabelOptions[].valuePrefix`\nand `options.publishLabelOptions[].valueSuffix`.\n\nThe property value corresponds to one of the options that appear in **Display Units** dropdown list in the **Visualization Options** pane. \nTo select this pane, click the **Configure plot** icon on the right-hand side of the **Plot Editor** options bar.\n\nNote: Not available when the plot type is `Text`. The API returns an HTTP response code `400`.\n",
            "type": "string",
            "enum": [
              "Bit",
              "Kilobit",
              "Megabit",
              "Gigabit",
              "Terabit",
              "Petabit",
              "Exabit",
              "Zettabit",
              "Yottabit",
              "Byte",
              "Kibibyte",
              "Mebibyte",
              "Gibibyte",
              "Tebibyte",
              "Pebibyte",
              "Exbibyte",
              "Zebibyte",
              "Yobibyte",
              "Nanosecond",
              "Microsecond",
              "Millisecond",
              "Second",
              "Minute",
              "Hour",
              "Day",
              "Week"
            ]
          },
          "yAxis": {
            "title": "Y axis position for plot",
            "description": "Y-axis position in plot\n",
            "x-description": "Position of the Y-axis for the plot to which this\n`publishLabelOptions` object applies. If `yAxis` is 0, the axis appears on the\nleft side. Otherwise it appears on the right. The default is 0 (left\nside).\n\nNote: Use only when `options.type` property is `TimeSeriesChart`, `List`, or `SingleValue`;\notherwise, the API returns an HTTP response code `400`.\n          \n",
            "type": "integer",
            "default": 0,
            "minimum": 0,
            "maximum": 1
          }
        },
        "required": [
          "label"
        ]
      },
      "Options": {
        "type": "object",
        "title": "Chart options",
        "properties": {
          "areaChartOptions": {
            "title": "Properties specific to area charts",
            "type": "object",
            "properties": {
              "showDataMarkers": {
                "title": "Controls the display of data markers for area charts",
                "type": "boolean",
                "description": "Controls appearance of markers for data arrival times\n",
                "x-description": "If `options.type` is `TimeSeriesChart` and `options.defaultPlotType` \nproperty is `AreaChart`, then `areaChartOptions.showDataMarkers` \ncontrols the appearance of small dots in the chart indicating\ntimes at which data arrived. To show markers, set to `true`;\notherwise, set to `false`. \n\nIf you use \n`options.areaChartOptions.showDataMarkers` with any other values of\n`options.type` and `options.defaultPlotType`, the API returns an\nHTTP response code `400`.\n",
                "default": false
              }
            }
          },
          "axes": {
            "title": "Axes options",
            "description": "Time series chart axis options\n",
            "x-description": "Axis options for the left and right side of a chart with `options.type` of\n`TimeSeriesChart`. The object in the first element specifies options\nfor the left chart axis, and the object in the second element specifies\noptions for the right chart axis. All other elements are ignored.\n\nNote: Use only when `options.type` is `TimeSeriesChart`;\notherwise, the API returns an HTTP response code `400`.\n",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ChartAxes"
            }
          },
          "axisPrecision": {
            "title": "Specifies the number of digits displayed for values plotted on the\nchart.\n",
            "description": "Precision of plot values\n",
            "x-description": "Specifies the digits Splunk Observability Cloud displays for values plotted on the\nchart. Choose a value that is sensible for the data; that is, ensure\nthat the variations in the data are discernible using the specified\nprecision. For example, if the data values usually fluctuate between\n100000 and 100010, using a precision of 3 would result in a value of\n100000 for every data point. Setting the precision to 6 would\ndistinguish between 100001 and 100002.\n\nNote: Use only when `options.type` property is `TimeSeriesChart`. Otherwise, the API returns an HTTP response code `400`.\n",
            "type": "integer",
            "minimum": 3,
            "maximum": 10,
            "default": 3
          },
          "colorBy": {
            "title": "Mechanism by which color is applied to values in the chart.\n",
            "description": "Sets mechanism for coloring values\n",
            "x-description": "Specifies how to apply a color scheme to the values in the chart. If\nyou want to apply color in a text chart, use HTML within the\n`markdown` property. The values you can use depend on the type of\nchart you specify in `options.type`:\n\n  * `Text`: The API returns an HTTP response code `400`.\n  * `Heatmap`: The `Range` and `Scale` enumerated types are allowed; the default is `Range`.\n  * `List`: The `Dimension`, `Metric`, and `Scale` enumerated types are allowed; the default is `Metric`.\n  * `SingleValue`: The `Dimension`, `Metric`, and `Scale` enumerated types are allowed; the default is `Metric`.\n  * `TimeSeriesChart`: The `Dimension` and `Metric` enumerated types are allowed; the default is `Dimension`.\n",
            "type": "string",
            "enum": [
              "Range",
              "Scale",
              "Dimension",
              "Metric"
            ]
          },
          "colorRange": {
            "title": "Heatmap color range options",
            "description": "Heatmap color range options\n",
            "x-description": "Properties that specify the range of color values to use in a\nheatmap chart, in the form of a JSON object. The properties specify\nthe lowest and highest color to use. `options.colorScale`\ncontrols how Splunk Observability Cloud sets each intermediate chart color\nwithin the range.\n\nNote: Use only when `options.type` property is `Heatmap`. Otherwise, the API returns an HTTP response code `400`.\n",
            "type": "object",
            "properties": {
              "color": {
                "title": "Starting color for a heatmap chart color range",
                "description": "Heatmap starting color\n",
                "x-description": "The starting color value for data values in a heatmap chart.\nSpecify the value as a 6-character hexadecimal value preceded by the \"#\"\ncharacter.\n\nThe `options.colorScale` property controls how Splunk Observability Cloud creates\na range of colors to represent different data values;\n`options.colorRange.color` is the starting point of the range,\nand `options.colorScale` controls the number of different\ncolors.\n\nThe UI limits you to hexadecimal values for the colors displayed in\ntable 5 in the section [Chart color palettes](https://dev.splunk.com/observability/docs/chartsdashboards/charts_overview#Chart-color-palettes)\nof the Charts API guide.\nUse the same values for API requests to keep your chart settings consistent with the UI.\n",
                "type": "string",
                "minLength": 7
              },
              "max": {
                "title": "Maximum color value assigned to a data value in the heatmap\nchart.\n",
                "description": "Color of largest data value\n",
                "x-description": "The color value to associate with the largest incoming data value\nin the heatmap chart.\n\nTo learn more about this property, see the description of `options.colorRange.color`.  \n",
                "type": "string",
                "minLength": 7
              },
              "min": {
                "title": "Minimum color value assigned to a data value in the heatmap\nchart.\n",
                "description": "Color of smallest data value\n",
                "x-description": "The color value associated with the smallest incoming data value\nin the heatmap chart.\n\nTo learn more about this property, see the description of `options.colorRange.color`.  \n",
                "type": "string",
                "minLength": 7
              }
            }
          },
          "colorScale": {
            "title": "Heatmap chart color range limits\n",
            "description": "Range limits for heatmap chart color ranges\n",
            "x-description": "Properties that specify the limits of the color ranges specified\nin `options.colorRange`, in the form of a JSON object. The\nproperties also control the order of colors in the color gradient.\nThe properties in `options.colorScale2` override any properties you\nspecify here.\n\nNote: Use only when `options.type` property is `Heatmap`. Otherwise, the API returns an HTTP response code `400`.\n",
            "type": "object",
            "properties": {
              "inverted": {
                "title": "Determines if colors are applied in reverse order from the\ndefault for the app.\n",
                "description": "Controls how colors apply to heatmap chart values\n",
                "x-description": "Determines how to use colors specified in `options.colorRange`.\nIf you set this property to `true`, and you also set `options.colorBy`\nto `Range`, darker colors represent smaller data values. For\nchart types other than `Heatmap`, setting this property to `true`\nresults in red representing lower values and green representing\nhigher ones (if the default color scheme is in use).\n",
                "type": "boolean",
                "default": false
              },
              "thresholds": {
                "title": "Specifies data values that partition the data in the chart.",
                "description": "Data values that map to color gradient values\n",
                "x-description": "Specifies data values that map to color gradient values. Specify\nthe values from lowest to highest. Data values that outside the\nspecified partitions don't appear in color, so you should set\nthe first array value to correspond to the lowest expected data\nvalue. Similarly, set the last array value to correspond to the\nhighest expected data value.\n\nNote: Splunk Observability Cloud uses only the first six elements in the array.\n",
                "type": "array",
                "items": {
                  "type": "number",
                  "format": "float"
                }
              }
            }
          },
          "colorScale2": {
            "title": "List of secondary visualization color properties",
            "description": "List of color range settings\n",
            "x-description": "List of secondary visualization color properties, in the form of an array\nof JSON objects. Each object in the array specifies the information\nfor a single color range, including the color to display for that\nrange and the borders of the range. The array specifies the entire\nrange displayed in the secondary visualization or heatmap chart. You\ndon't need to insert the elements in a specific order, because\nautomatically orders them by the values in the display. The lowest\nvalue becomes the left border of the secondary visualization, and\nthe highest value becomes the right border.\n",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/SecondaryVisualization"
            }
          },
          "defaultPlotType": {
            "title": "Specifies the visualization option to use for a graph",
            "description": "Default type for plots in this chart\n",
            "x-description": "When you choose a graph chart type by setting `options.type` to `TimeSeriesChart`,\n`options.defaultPlotType` sets the visualization to use for\nplots in the chart:\n\n  * `LineChart`: A plot with data points connected by a\n    series of straight lines.\n  * `AreaChart`: Similar to a LineChart, with the area between\n    the plot line and the x-axis filled with the line color.\n  * `Column`: Shaded vertical bars with their bottom on the\n    x-axis and their top at the data points value. The bars aren't\n    connected. Also known as a bar chart.\n  * `Histogram`: Colored bars that indicate the distribution of data points values\n    at a point in time.\n",
            "type": "string",
            "enum": [
              "LineChart",
              "AreaChart",
              "ColumnChart",
              "Histogram"
            ],
            "default": "LineChart"
          },
          "groupBy": {
            "title": "Groups of histogram chart properties\n",
            "description": "Histogram chart properties\n",
            "x-description": "Specifies a list of custom properties to use in grouping values in a histogram chart,\nin the form of a JSON array of strings. Each element must contain a key specified\nin `customProperties` or a valid default custom property. The first\nelement determines the top-level grouping, and the second element determines the second-level\ngrouping within each first-level grouping. All other elements are ignored. \nThe section [Heatmap chart grouping](https://help.splunk.com/?resourceId=data-visualization_charts_chart-types#de2309d8245aa4252814157eb0c3ffe5f__heatmap-charts-grouping)\nin API topics shows a heatmap that charts CPU utilization, using AWS availability zone as the top-level grouping \nand number of host CPU cores as the second-level grouping.\n\nNote: Use only when `options.type` property is `Heatmap` or `TableChart`. Otherwise, the API returns an HTTP response code `400`.\n",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "groupBySort": {
            "title": "Table chart options\n",
            "description": "Sorts groups of values in table charts\n",
            "x-description": "Sorts a specified group of values in a table chart. \nGroups can either be sorted by `Ascending` or `Descending` order.\n\nNote: Use only when `options.type` property is `TableChart`. Otherwise, the API returns an HTTP response code `400`.\n",
            "type": "string",
            "enum": [
              "Ascending",
              "Descending"
            ]
          },
          "hideMissingValues": {
            "title": "Table chart options\n",
            "description": "Option to exclude missing metric values\n",
            "x-description": "Determines whether to include missing metric values in a table chart or list chart. \nIf `true`, MTS with missing values are excluded from the chart.\n\nNote: Use only when `options.type` property is `List` or `TableChart`. Otherwise, the API returns an HTTP response code `400`.\n",
            "type": "boolean",
            "default": false
          },
          "histogramChartOptions": {
            "type": "object",
            "properties": {
              "colorThemeIndex": {
                "title": "Color to use for data points in a histogram chart",
                "description": "Color palette index for histogram color theme\n",
                "x-description": "Color palette index that points to the theme color of a histogram chart. Use one of the first 16 index values in\ntable 5 in the section [Chart color palettes](https://dev.splunk.com/observability/docs/chartsdashboards/charts_overview#Chart-color-palettes)\nof the Charts API guide.\n\nNotes:\n\n  * Use only when `options.type` is `TimeSeriesChart` and `options.defaultPlotType` is `Histogram`;\n    otherwise, the API returns an HTTP response code `400`.\n  * Your users may see colors other than those shown in \n    table 5 in the section [Chart color palettes](https://dev.splunk.com/observability/docs/chartsdashboards/charts_overview#Chart-color-palettes),\n    depending on the settings they select for color blindness. To learn more, see \n    table 6 in the section [Chart color palettes](https://dev.splunk.com/observability/docs/chartsdashboards/charts_overview#Chart-color-palettes).\n",
                "type": "integer",
                "minimum": 0,
                "maximum": 15,
                "default": 15
              }
            }
          },
          "includeZero": {
            "title": "Determines how 0 affects Y-axis values",
            "description": "Controls how 0 value affects Y-axis\n",
            "x-description": "Determines how 0 is treated when dynamically calculating Y-axis\nvalues. If `true` 0 is included in the calculation. Otherwise,\nSplunk Observability Cloud ignores data points containing 0. This is a chart-wide\nsetting applied to all the axes. However, if `options.axes.min` or\n`options.axes.max` exclude 0 from the displayed range for an axis, 0\nisn't displayed on that axis even if `includeZero` is `true`.\n\nNote: Use only when `options.type` property is `TimeSeriesChart`. Otherwise, the API returns an HTTP response code `400`.\n",
            "type": "boolean",
            "default": false
          },
          "legendOptions": {
            "title": "Table options",
            "description": "Table options\n",
            "x-description": "Sets options for the data table of a chart or a table chart, including which properties are\nomitted.\n",
            "type": "object",
            "properties": {
              "fields": {
                "title": "List of table settings for properties in the chart",
                "description": "Data table settings\n",
                "x-description": "A list of table settings for properties in the chart, in\nthe form of a JSON array. Each element of the array is a JSON\nobject that contains a chart property key name and a flag. If\nthe flag is `true`, the chart property appears;\notherwise, it's hidden.\n",
                "type": "array",
                "items": {
                  "type": "object",
                  "properties": {
                    "enabled": {
                      "title": "Determines property display",
                      "description": "Controls property display\n",
                      "x-description": "Determines if the property specified by\n`legendOptions.properties.property` for this element is\ndisplayed in the table.\n\nNote: Use only when `options.type` property is `TimeSeriesChart`, `TableChart`, `List`,\nor `SingleValue`. Otherwise, the API returns an HTTP response code `400`.\n",
                      "type": "boolean",
                      "default": true
                    },
                    "property": {
                      "title": "Key name of a property specified for this chart",
                      "description": "Name of a property to hide or show\n",
                      "x-description": "The key name of a property to hide or show in the data\ntable\n",
                      "type": "string"
                    }
                  }
                }
              }
            }
          },
          "lineChartOptions": {
            "type": "object",
            "title": "Line graph options",
            "description": "Line chart options\n",
            "x-description": "Specifies options for a line graph chart\n\nNote: Use only when `options.type` property is `TimeSeriesChart` and\n`options.defaultPlotType` is `LineChart`. Otherwise, the API returns an HTTP response code `400`.\n",
            "properties": {
              "showDataMarkers": {
                "title": "Determines how to represent data points in the chart",
                "description": "Controls display of data points\n",
                "x-description": "Determines how to represent individual data points in the chart.\nIf `true`, the chart is displayed with filled circles for each\ndata points. Otherwise, the chart is displayed as a smooth line.\n",
                "type": "boolean",
                "default": false
              }
            }
          },
          "markdown": {
            "title": "Text chart contents",
            "description": "Text chart contents\n",
            "x-description": "The contents of a text chart, using GitHub-Flavored Markdown\n(**GFM**) or HTML.\n\nNote: Use only when `options.type` property is `Text`. Otherwise, the API returns an HTTP response code `400`.\n",
            "type": "string"
          },
          "maximumPrecision": {
            "title": "Specifies the number of fractional digits used to display data",
            "description": "Number of significant digits for data points\n",
            "x-description": "Indicates the number of significant digits to the right of the\ndecimal point to use for data points. The integer part of the\ndata points is used, regardless of the specified precision. Choose a\nvalue so that variations in the data are visible for the specified\nprecision. For example, if data values are in the range 0.001 to\n0.01, set `maximumPrecision` to 4. If you don't specify a value,\nSplunk Observability Cloud adjusts the precision to fit the available display\narea.\n\nNote: Use only when `options.type` is `List`, `TableChart`, or `SingleValue`. Otherwise, the API returns an HTTP response code `400`.\n",
            "type": "integer",
            "minimum": 0
          },
          "noDataOptions": {
            "title": "User-defined message for when a chart has no active metrics",
            "description": "Set up a message that appears when metrics used in a chart either don't exist or are inactive. \n",
            "x-description": "Set up a message that appears when metrics used in a chart either don't exist or are inactive.\n\nUse only when `options.type` is `Heatmap`, `List`, `SingleValue`, or `TimeSeriesChart`. Otherwise, the API returns an HTTP response code `400`.\n\nA metric is considered inactive by Splunk Observability Cloud in the following cases: \n\n* The metric hasn't received any data for 24 hours.\n* The metric is tagged as `ephemeral` and hasn't received any data for one hour.\n\nNote: A chart with inactive metrics is distinct from a chart with active metrics that doesn't receive data. For example, a chart might not receive any data despite using active metrics if you use a filter on the chart that doesn't match any data. On a chart with active metrics, the no active metrics message won't appear even if the chart isn't receiving any data.\n\nTo learn more about why your message might not appear, see [When your message doesn't appear](https://help.splunk.com/?resourceId=data-visualization_charts_chart-options#dbcbe241b94ca404ea776a186bc9ca2f2__message-does-not-appear) in Splunk Observability Cloud user documentation.\n",
            "properties": {
              "noDataMessage": {
                "title": "Display message",
                "description": "Message to show in a chart when no active metric is available.\n",
                "x-description": "Message to show in a chart when no active metric is available. Max length is 140 characters.\n",
                "type": "string",
                "example": "No active hosts found"
              },
              "linkUrl": {
                "title": "URL to additional resource",
                "description": "URL that links to resource for additional information.\n",
                "x-description": "URL that links to resource for additional information.\n",
                "type": "string",
                "example": "https://docs.splunk.com/Observability/gdi/opentelemetry/opentelemetry.html"
              },
              "linkText": {
                "title": "Display text for the URL",
                "description": "Display text for the URL that links to additional resource.\n",
                "x-description": "Display text for the URL that links to additional resource. Max length is 50 characters.",
                "type": "string",
                "example": "Set up Splunk OpenTelemetry"
              }
            }
          },
          "onChartLegendOptions": {
            "type": "object",
            "title": "Chart legend options",
            "description": "Controls display of legends\n",
            "x-description": "Properties that determine if Splunk Observability Cloud should display a legend, and if so,\nwhich dimension to show in the legend, in the form of a JSON object.\n\nNote: Use only when `options.type` is `TimeSeriesChart`, `List`,\nor `SingleValue`. Otherwise, the API returns an HTTP response code `400`.\n",
            "properties": {
              "dimensionInLegend": {
                "title": "Dimension to show in the legend",
                "description": "Dimension to show in the legend\n",
                "x-description": "Dimension to show in the legend. Ensure that the dimension\nexists and has different values for each set of data points in\nthe chart.\n\nNote: Use only when `options.type` property is `TimeSeriesChart`. Otherwise, the API returns an HTTP response code `400`.\n",
                "type": "string"
              },
              "showLegend": {
                "title": "Determines if a legend show display with the chart",
                "description": "Controls display of legend\n",
                "x-description": "Determines if Splunk Observability Cloud should display a legend with the chart.\nIf `true`, a legend is displayed below the chart. This legend\nlists the each value of the dimension specified in the\n`options.onChartLegendsOptions.dimensionInLegend` property. Next\nto each value is the color of the data point that has that\ndimension value in the chart.\n\nNote: Use only when `options.type` property is `TimeSeriesChart`. Otherwise, the API returns an HTTP response code `400`.\n",
                "type": "boolean",
                "default": false
              }
            }
          },
          "programOptions": {
            "title": "General chart options",
            "description": "General appearance and operation options\n",
            "x-description": "Options that control the general appearance and operation\nof the chart, in the form of a JSON object.\n\nNote: Available for any value of `options.type` except `Text`\n",
            "type": "object",
            "properties": {
              "disableSampling": {
                "title": "Determines if the chart uses sampling",
                "description": "Controls sampling display\n",
                "x-description": "Determines if the chart displays samples from the data or plots\nevery data points. By default, Splunk Observability Cloud uses sampling to provide\nbetter performance for charts.\n\nNote: Available for any value of `options.type` except `Text`\n",
                "type": "boolean",
                "default": false
              },
              "maxDelay": {
                "title": "Interval to wait before charting the available data",
                "description": "In milliseconds, the time to wait before ignoring future data points\n",
                "x-description": "Time in milliseconds to wait before charting the available data\nand ignoring points that arrive afterwards. By default, Splunk Observability Cloud\nautomatically detects and applies a sensible value. You can also\nchoose the automatic option by explicitly setting `maxDelay` to `0`.\nThe maximum value is 900,000 ms (15 minutes).\n\nNote: Available for any value of `options.type` except `Text`\n",
                "type": "integer",
                "format": "int32",
                "minimum": 0,
                "maximum": 900000
              },
              "minimumResolution": {
                "title": "Minimum data resolution to use",
                "description": "Minimum computation resolution\n",
                "x-description": "Minimum resolution to use when computing the SignalFlow program,\nspecified in milliseconds. By default, Splunk Observability Cloud computes and\napplies a sensible value. You can also choose the automatic\noption by explicitly setting `minimumResolution` to 0. The\nmaximum value is 900,000 ms (15 minutes).\n\nNote: Available for any value of `options.type` except `Text`\n",
                "type": "integer",
                "minimum": 0,
                "maximum": 900000
              },
              "timezone": {
                "title": "Time zone for SignalFlow calendar window transformations",
                "description": "Time zone for calendar window transformations\n",
                "x-description": "Time zone that SignalFlow uses as the basis of calendar window\ntransformation methods. For example, if you set `\"timezone\":\n\"Europe/Paris\"` and then use the transformation\n`sum(cycle=\"week\", cycle_start=\"Monday\")` in the\nSignalFlow program for your chart, the calendar window starts on Monday, Paris \ntime.\n\nTo learn more, see [Timezones and SignalFlow calendar window transformations](https://dev.splunk.com/observability/docs/chartsdashboards/charts_overview#Timezones-and-SignalFlow-calendar-window-transformations).\n",
                "type": "string",
                "default": "UTC"
              }
            }
          },
          "publishLabelOptions": {
            "title": "SignalFlow statement options",
            "description": "List of options for each plot in the chart\n",
            "x-description": "List that specifies options for each plot displayed in the chart, in\nthe form of an array of JSON objects.\n\nIn this context, a plot is the `publish()` block output from a \nsingle statement in the SignalFlow program for the chart. \n\nTo match a SignalFlow statement to an array element, Splunk Observability Cloud maps\nthe value of the `label` argument for the `publish()` block to the value of a\n`publishLabelOptions[].label` property.\n\nSome `publishLabelOptions` child object properties aren't supported\nfor all plot types.\n\nNote: Not available when `options.type` is `Text`; the API returns an\nHTTP response code `400`.\n",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PublishLabelOptions"
            }
          },
          "refreshInterval": {
            "title": "Interval at which Splunk Observability Cloud should update the chart with new data",
            "description": "In milliseconds, refresh interval for chart\n",
            "x-description": "How often, in milliseconds, that Splunk Observability Cloud should refresh the chart\nwith data. If Splunk Observability Cloud can detect the resolution of the data, the\ndefault is 10,000 (10 seconds). Otherwise, the default is 3,600,000\n(1 hour).\n\nNote: Use only when `options.type` property is `List`, `TableChart`, or `SingleValue`. Otherwise, the API returns an HTTP response code `400`.\n",
            "type": "integer",
            "format": "int32",
            "minimum": 0
          },
          "secondaryVisualization": {
            "title": "Secondary visualization to use with the chart",
            "description": "Secondary visualization for list or single value chart\n",
            "x-description": "Secondary visualization to use with list or single value charts.\nSee the section [Secondary visualization](https://help.splunk.com/?resourceId=data-visualization_charts_chart-options#dbcbe241b94ca404ea776a186bc9ca2f2__secondary)\nfor examples of the available visualizations.\n\nNote: Use only when `options.type` property is `List` or `SingleValue`. Otherwise, the API returns an HTTP response code `400`.\n",
            "type": "string",
            "enum": [
              "None",
              "Radial",
              "Linear",
              "Sparkline"
            ]
          },
          "showEventLines": {
            "title": "Specifies what to show on a chart when an event occurs",
            "description": "Controls display of vertical line when an event occurs\n",
            "x-description": "If `true`, Splunk Observability Cloud displays a vertical line on the chart at the\npoint that an event occurs.\n\nNote: Use only when `options.type` property is `TimeSeriesChart`;\notherwise, the API returns an HTTP response code `400`.\n",
            "type": "boolean",
            "default": false
          },
          "showSparkLine": {
            "title": "Determines if Splunk Observability Cloud should show a sparkline in a single value\nchart.\n",
            "description": "Controls the display of a sparkline in a single value chart\n",
            "x-description": "If `true`, Splunk Observability Cloud displays a sparkline for the value shown in a\nsingle value chart.\n\nNotes:\n\n  * Use only when `optionstype` property is `SingleValue`. Otherwise, the API returns an HTTP response code `400`.\n  * The value specified for `options.secondaryVisualization`\n    overrides `options.showSparkLine`.\n",
            "type": "boolean",
            "default": false
          },
          "sortBy": {
            "title": "Specifies how to sort the entries in a time series chart or table chart",
            "description": "Sort order of chart entries\n",
            "x-description": "Specifies how to sort the entries in a time series chart or table chart. The first\ncharacter of the value must be one of the following:\n\n  * Minus sign (`-`): Sets descending order for the data\n  * Plus sign (`+`): Sets ascending order for the data\n\nThe rest of the characters specify the sort criteria:\n\n  * A keyword that represents a data points value, a metric, or the `publish()` label of the SignalFlow statement that\n    generates the data\n  * A dimension that's available to the chart.\n\nThe default is to sort on the `publish()` label in ascending\norder; this corresponds to the **Auto** option in the Splunk Observability Cloud web\nUI.\n\nValues that don't map to a keyword or valid dimension are\nsorted with the default specification.\nData that contains `null` sorts to the start of the chart for ascending order and to the\nend of the list for descending order.\n\nNote: Use only when `options.type` property is `TimeSeriesChart` or `TableChart`. Otherwise, the API returns an HTTP response code `400`.\n",
            "type": "string"
          },
          "sortDirection": {
            "title": "Specifies how to sort values in a heatmap chart",
            "description": "Sort direction of heatmap chart entries\n",
            "x-description": "If set to `Ascending`, the chart displays values from low to high;\notherwise, the chart displays values from high to low.\n\nNote: Use only when `options.type` property is `Heatmap`. Otherwise, the API returns an HTTP response code `400`.\n",
            "type": "string",
            "enum": [
              "Ascending",
              "Descending"
            ],
            "default": "Ascending"
          },
          "sortProperty": {
            "title": "A custom property to use as the sort criteria for a heatmap chart",
            "description": "Custom property to sort on for a heatmap chart\n",
            "x-description": "The custom property to use as the sorting criteria for the heatmap\nchart. The value should map to a valid key name specified in the\ncustomProperties property or to a valid custom property available by\ndefault. If no value is specified, the values are displayed without\nan obvious order. The name of a custom property to use as the sort\ncriteria for a heatmap chart. The value must be a valid key\nspecified by the `customProperty` property or a valid default custom\nproperty. If you don't specify a value, the chart displays the\nvalues without any obvious order.\n\nNote: Use only when `options.type` property is `Heatmap`. Otherwise, the API returns an HTTP response code `400`.\n",
            "type": "string"
          },
          "stacked": {
            "title": "Controls the display of stacked plots in a graph",
            "description": "Controls display of stacked plots\n",
            "x-description": "Controls the display of stacked plots in a graph. If\n`true`, Splunk Observability Cloud stacks the plots and adds data points together to\ncreate the values indicated by the axis labels. You can still see\nindividual values when you highlight them within the colored area\nrepresenting a single plot. You can also view individual values in\nthe data table.\n\nIf `options.defaultPlotType` is 'AreaChart`, Splunk Observability Cloud\ndisplays the plots as stacked areas. If `defaultPlotType` is `Column`,\nSplunk Observability Cloud displays the plots as stacked columns.\n\nNote: Use only when `options.type` property is `TimeSeriesChart`\nand `options.defaultPlotType` is `AreaChart` or `Column`. Otherwise, \nthe API returns an HTTP response code `400`.\n",
            "type": "boolean",
            "default": false
          },
          "time": {
            "title": "Chart time display options",
            "description": "Time-related properties\n",
            "x-description": "Time-related properties for a chart, in the form of a JSON\nobject.\n\nNote: Use only when `options.type` property is `TimeSeriesChart`. Otherwise, the API returns an HTTP response code `400`.\n",
            "type": "object",
            "properties": {
              "end": {
                "title": "Timestamp of the last time to display in the chart",
                "description": "Last timestamp to display\n",
                "x-description": "The timestamp of the last time to display in the chart, in *nix\ntime\n\nNote: This option is only available if\n`options.type` is `TimeSeriesChart` and `options.time.type` is\n`absolute`.\n",
                "type": "integer",
                "format": "int64",
                "example": 205430400000
              },
              "range": {
                "title": "Number of milliseconds to display in the chart",
                "description": "Number of milliseconds to display\n",
                "x-description": "The number of milliseconds to display in the chart. From this\nvalue, Splunk Observability Cloud calculates a rolling range with the current time\nat the right edge of the chart. Use 0 to use the default\nbehavior, which is -15m except for the following metrics :\n  \n  * AWS: See the metrics listed in the [Connect to AWS](https://quickdraw.splunk.com/redirect/?product=Observability&location=apiref.charts.object.options.time.range.aws.metrics&version=current) topic in the user documentation.\n  * GCP: See the metrics listed in the [Connect to GCP](https://quickdraw.splunk.com/redirect/?product=Observability&location=apiref.charts.object.options.time.range.gcp.metrics&version=current) topic\n    in the user documentation.\n  * Microsoft Azure: See the metrics listed in the [Connect to Azure](https://quickdraw.splunk.com/redirect/?product=Observability&location=apiref.charts.object.options.time.range.azure.metrics&version=current) topic\n    in the user documentation.\n\nNote: Use only when `options.type` property is `TimeSeriesChart` and\n`options.time.type` is `relative. Otherwise, the API returns an HTTP response code `400`.\n",
                "type": "integer",
                "format": "int64",
                "minimum": 0,
                "default": 0
              },
              "start": {
                "title": "Timestamp of the first time to display in the chart",
                "description": "First timestamp to display\n",
                "x-description": "The timestamp of the first time to display in the chart, in *nix\ntime\n\nNote: Use only when `options.type` property is `TimeSeriesChart`\nand `options.time.type` is `absolute`. Otherwise, the API returns an HTTP response code `400`.\n",
                "type": "integer",
                "format": "int64",
                "example": 205344000000
              },
              "type": {
                "title": "Determines the type of time range to use",
                "description": "Use explicit time range or show the last n milliseconds\n",
                "x-description": "Determines whether to use an explicit time range or to show a\nrange of the last *n* milliseconds.\n\nNote: Use only when `options.type` property is `TimeSeriesChart`. Otherwise, the API returns an HTTP response code `400`.\n",
                "type": "string",
                "enum": [
                  "relative",
                  "absolute"
                ],
                "default": "relative"
              }
            }
          },
          "timeStampHidden": {
            "title": "Determines if the timestamp is shown with displayed values",
            "description": "Controls timestamp display with values\n",
            "x-description": "Determines if the timestamp displays with displayed values. If\n`true`, the timestamp for a value appears with the value in the\nchart.\n\nNote: Use only when `options.type` property is `Heatmap`, `TableChart`, or\n`SingleValue`. Otherwise, the API returns an HTTP response code `400`.\n",
            "type": "boolean",
            "default": false
          },
          "type": {
            "title": "Visualization type",
            "description": "Type of chart to display\n",
            "x-description": "Type of visualization to use for the data. Splunk Observability Cloud provides six\nmain visualization types:\n\n  * `Heatmap`: Displays data in a set of colored boxes. You can provide an optional order and grouping;\n    otherwise, each box represents a metric time series in the output. The color of a box is determined by the\n    value of the metric it represents. Splunk Observability Cloud provides default values, or you can specify your own custom ranges.\n  * `List`: Displays data as a list of metric time series. You can provide an additional secondary visualization\\\n    as well. For example, you can display a sparkline for each row in the list.\n  * `SingleValue`: Displays a single value with a color that you specify. You can specify an additional\n    visualization (secondary visualization) as well. For example, you can display a sparkline next to the value.\n  * `TableChart`: Displays metrics and dimensions in table format. Each metric name and dimension key displays as a column. Each\n    output MTS displays as a row. If there are multiple values for a cell, each MTS displays in a separate row. \n  * `Text`: Displays text you provide. You can use Markdown to format the appearance of this text.\n  * `TimeSeriesChart`: Displays graphs of metric time series over time, using a graph type determined by the\n    value of `options.defaultPlotType` or `options.publishLabelOptions.plotType`:\n\n    * `LineChart`: Displays data points as plot points connected by straight lines. In a graph that displays multiple\n      metric time series, such as metric data points for several dimension values, Splunk Observability Cloud assigns a different color to each\n      MTS.\n    * `AreaChart`: Similar to a LineChart, but the area between the line and the\n      X-axis is filled with the color of the line. You can display multiple MTS as stacked area charts by setting `options.stacked`\n      to `true`.\n    * `Column`: Displays each data points in an MTS as a colored vertical bar with the base on the X-axis and\n      the top at the data points value. This type is also known as a bar chart. In a chart that displays multiple MTS, each\n      data points for each MTS has its own bar and bar color, and the bars are clustered around the data points timestamp. You can\n      display multiple MTS as stacked bars by setting `options.stacked`\n      to `true`.\n    * `Histogram`: Colored bars that indicate the distribution of data points values\n      at a point in time.\n",
            "type": "string",
            "enum": [
              "Heatmap",
              "List",
              "SingleValue",
              "TableChart",
              "Text",
              "TimeSeriesChart"
            ]
          },
          "unitPrefix": {
            "title": "Determines whether to use metric or binary units for chart values",
            "description": "Measure to use when displaying or labeling values\n",
            "x-description": "Specifies the type of unit to use when displaying or labeling values\n    - Metric: Values represent decimal multiples. For example, if `unitPrefix` is Metric, 1K represents 1 kilobyte or 1000\n      bytes.\n    - Binary: Values represent binary multiples. For example, if `unitPrefix` is Binary, 1K represents 1 kibibyte or 1024\n      bytes.\n      \nNote: This option is available for any value of `option.type` except `Text`.\n",
            "type": "string",
            "enum": [
              "Metric",
              "Binary"
            ],
            "default": "Metric"
          }
        }
      },
      "Created": {
        "title": "Chart creation time",
        "type": "integer",
        "format": "int64",
        "description": "Chart creation time, in *nix format. Set by system.\n",
        "x-description": "The time the chart was created, in *nix time in milliseconds.\nThis value is always set by the system.\n",
        "example": 1557534630000,
        "readOnly": true
      },
      "Creator": {
        "title": "Chart Creator ID",
        "description": "User ID of chart creator. Set by system.\n",
        "x-description": "ID of the user that initially created the chart\n",
        "type": "string",
        "readOnly": true
      },
      "ChartID": {
        "title": "Chart ID",
        "description": "Chart identifier. Set by system.\n",
        "x-description": "System-defined identifier for the chart\n",
        "type": "string",
        "readOnly": true
      },
      "LastUpdated": {
        "title": "Chart last updated time",
        "description": "Chart last updated time, in *nix format. Set by system.\n",
        "x-description": "The last time the chart was updated, in *nix time in milliseconds.\nThis value is always set by the system.\n",
        "type": "integer",
        "format": "int64",
        "example": 1557707430000,
        "readOnly": true
      },
      "LastUpdatedBy": {
        "title": "Chart last updated ID",
        "description": "ID of user who last updated the chart. Set by system.\n",
        "x-description": "The ID of the last person who updated the chart. If the\nlast update was done by the system, the value is the string literal\n\"AAAAAAAAAA\".\n",
        "type": "string",
        "readOnly": true
      },
      "RelatedDetectorIds": {
        "title": "Detectors linked to this chart",
        "description": "List of detectors linked to this chart.\n",
        "x-description": "List of detectors linked to this chart, in the form of a JSON array. Each item is a unique ID of a linked detector. This list is read-only and is retrieved from `programText`.\n",
        "type": "array",
        "items": {
          "type": "string"
        },
        "uniqueItems": true,
        "readOnly": true
      },
      "AutoDetectRelatedDetectorIds": {
        "title": "AutoDetect detectors linked to this chart",
        "description": "List of AutoDetect and customized AutoDetect detectors linked to this chart.\n",
        "x-description": "List of AutoDetect and customized AutoDetect detectors linked to this chart, in the form of a JSON array. Each item is a unique ID of a linked AutoDetect or customized AutoDetect detector. This list is read-only and is retrieved from `programText`.\n",
        "type": "array",
        "items": {
          "type": "string"
        },
        "uniqueItems": true,
        "readOnly": true
      },
      "CreateUpdateChartRequest": {
        "title": "Properties for creating or updating a chart",
        "type": "object",
        "properties": {
          "customProperties": {
            "$ref": "#/components/schemas/CustomProperties"
          },
          "description": {
            "$ref": "#/components/schemas/Description"
          },
          "name": {
            "$ref": "#/components/schemas/Name"
          },
          "options": {
            "$ref": "#/components/schemas/Options"
          },
          "packageSpecifications": {
            "$ref": "#/components/schemas/PackageSpecifications"
          },
          "programText": {
            "$ref": "#/components/schemas/ProgramText"
          },
          "tags": {
            "$ref": "#/components/schemas/Tags"
          }
        }
      },
      "GetChartsResult": {
        "type": "object",
        "properties": {
          "created": {
            "$ref": "#/components/schemas/Created"
          },
          "creator": {
            "$ref": "#/components/schemas/Creator"
          },
          "customProperties": {
            "$ref": "#/components/schemas/CustomProperties"
          },
          "description": {
            "$ref": "#/components/schemas/Description"
          },
          "id": {
            "$ref": "#/components/schemas/ChartID"
          },
          "lastUpdated": {
            "$ref": "#/components/schemas/LastUpdated"
          },
          "lastUpdatedBy": {
            "$ref": "#/components/schemas/LastUpdatedBy"
          },
          "name": {
            "$ref": "#/components/schemas/Name"
          },
          "options": {
            "$ref": "#/components/schemas/Options"
          },
          "relatedDetectorIds": {
            "$ref": "#/components/schemas/RelatedDetectorIds"
          },
          "autoDetectRelatedDetectorIds": {
            "$ref": "#/components/schemas/AutoDetectRelatedDetectorIds"
          }
        }
      },
      "CreateUpdateChartResponse": {
        "type": "object",
        "title": "Response object for successful chart creation or update",
        "properties": {
          "created": {
            "$ref": "#/components/schemas/Created"
          },
          "creator": {
            "$ref": "#/components/schemas/Creator"
          },
          "customProperties": {
            "$ref": "#/components/schemas/CustomProperties"
          },
          "description": {
            "$ref": "#/components/schemas/Description"
          },
          "id": {
            "$ref": "#/components/schemas/ChartID"
          },
          "lastUpdated": {
            "$ref": "#/components/schemas/LastUpdated"
          },
          "lastUpdatedBy": {
            "$ref": "#/components/schemas/LastUpdatedBy"
          },
          "name": {
            "$ref": "#/components/schemas/Name"
          },
          "options": {
            "$ref": "#/components/schemas/Options"
          },
          "packageSpecifications": {
            "$ref": "#/components/schemas/PackageSpecifications"
          },
          "programText": {
            "$ref": "#/components/schemas/ProgramText"
          },
          "tags": {
            "$ref": "#/components/schemas/Tags"
          },
          "relatedDetectorIds": {
            "$ref": "#/components/schemas/RelatedDetectorIds"
          },
          "autoDetectRelatedDetectorIds": {
            "$ref": "#/components/schemas/AutoDetectRelatedDetectorIds"
          }
        }
      },
      "GetSingleChartResponse": {
        "type": "object",
        "title": "Response object for retrieving a single chart by ID",
        "properties": {
          "created": {
            "$ref": "#/components/schemas/Created"
          },
          "creator": {
            "$ref": "#/components/schemas/Creator"
          },
          "customProperties": {
            "$ref": "#/components/schemas/CustomProperties"
          },
          "description": {
            "$ref": "#/components/schemas/Description"
          },
          "id": {
            "$ref": "#/components/schemas/ChartID"
          },
          "lastUpdated": {
            "$ref": "#/components/schemas/LastUpdated"
          },
          "lastUpdatedBy": {
            "$ref": "#/components/schemas/LastUpdatedBy"
          },
          "name": {
            "$ref": "#/components/schemas/Name"
          },
          "options": {
            "$ref": "#/components/schemas/Options"
          },
          "packageSpecifications": {
            "$ref": "#/components/schemas/PackageSpecifications"
          },
          "programText": {
            "$ref": "#/components/schemas/ProgramText"
          },
          "tags": {
            "$ref": "#/components/schemas/Tags"
          },
          "relatedDetectorIds": {
            "$ref": "#/components/schemas/RelatedDetectorIds"
          },
          "autoDetectRelatedDetectorIds": {
            "$ref": "#/components/schemas/AutoDetectRelatedDetectorIds"
          }
        }
      }
    }
  }
}